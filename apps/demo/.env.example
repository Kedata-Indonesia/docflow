# Demo app environment
#
# Vite exposes only env vars prefixed with VITE_ to the client.
#
# For same-domain deployment (default), leave this empty / unset.
# For separate-domain deployment, set this to the backend base URL, e.g.:
#   VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
